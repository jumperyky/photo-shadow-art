# バッチファイルは CRLF でないと cmd.exe が正しく解釈しないことがある
# (特に goto やカッコで囲んだブロック)。チェックアウト時に必ず CRLF にする。
*.bat text eol=crlf
*.cmd text eol=crlf

# シェルスクリプトは逆に LF でなければ動かない
# (Windows でチェックアウトしても CRLF にしない)
*.sh text eol=lf
